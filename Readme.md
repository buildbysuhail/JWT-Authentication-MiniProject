## Tech Stack
- Node.js
- Express.js
- MongoDB Atlas
- Mongoose
- JWT Authentication
- Render (Deployment)

## Features
> User Registration & Login
> Password hashing using bcrypt
> JWT-based authentication
> Protected routes
> CRUD operations on posts (not completed)
> Search & filter using MongoDB aggregation (coming soon)
> Environment-based configuration

# This Mini Project is taken out from repo called 'Day-By-Day-Learning-of-MongoDB-' for Depl
## LOGIN FLOW
### POST /api/auth/login
→ server
→ login route
→ JWT created
→ token sent to client


### GPT > 
 ## What I recommend next 🚀

You can choose:
1️⃣ Add role-based auth (admin/user)
2️⃣ Add refresh tokens
3️⃣ Secure JWT using env variables
4️⃣ Combine Auth + Aggregation (protected search API)

